import {
  SCHOOL,
  URL1,
  URL2,
  URL3,
  HOST,
  PORT,
  DEFAULT_HEADERS,
  FILE_TYPE,
  BASE64_SANITIZE_REGEX,
  MONGO_URI,
  DB_NAME,
} from "./_constant.js";

export default {
  school: SCHOOL,
  host: HOST,
  port: PORT,
  mongoUri: MONGO_URI,
  dbName: DB_NAME,
  url1: URL1,
  url2: URL2,
  url3: URL3,
  defaultHeaders: DEFAULT_HEADERS,
  fileType: FILE_TYPE,
  regex: BASE64_SANITIZE_REGEX,
};
